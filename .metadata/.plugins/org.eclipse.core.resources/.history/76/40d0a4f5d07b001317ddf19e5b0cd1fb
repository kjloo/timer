import java.awt.Color;
import java.awt.Dimension;
import java.awt.DisplayMode;
import java.awt.Font;
import java.awt.Graphics;
import java.awt.Toolkit;

import javax.swing.JFrame;
import javax.swing.JLabel;
import javax.swing.SwingUtilities;

import net.miginfocom.swing.MigLayout;


public class ClockFrame extends JFrame{
	
	private ClockPanel clock;
	private int width = 800;
	private int height = 600;
	
	
	public ClockFrame() {
		this.clock = new ClockPanel();
		DisplayMode dm = new DisplayMode(width, height, 16, DisplayMode.REFRESH_RATE_UNKNOWN);
		ClockFrame test = new ClockFrame();
		test.initComponents(dm);
	}
	
	public ClockFrame(ClockPanel clock) {

		this.clock = clock;
		Dimension screenSize = Toolkit.getDefaultToolkit().getScreenSize();
		this.width = (int) screenSize.getWidth();
		this.height = (int) screenSize.getHeight();
		this.dm = new DisplayMode(width, height, 16, DisplayMode.REFRESH_RATE_UNKNOWN);
		initComponents(dm);
	}
	
	private void initComponents(DisplayMode dm) {
		//Settings
		setBackground(Color.WHITE);
		setForeground(Color.BLACK);
		setFont(new Font("Arial", Font.PLAIN, 24));
		Screen s = new Screen();
		
		try {
			s.setFullScreen(dm, this);
			try {
				Thread.sleep(3000);
			}catch(Exception ex) {
				ex.printStackTrace();
			}
		}finally {
			s.restoreScreen();
		}
	}
	
	public void paint(Graphics g) {
		g.drawString("This Doesn't Work!", 200, 200);
	}
	
	public static void main(String args[]) {
        SwingUtilities.invokeLater(new Runnable() {
            public void run() {
                new ClockFrame();
            }
        });
    }

}
