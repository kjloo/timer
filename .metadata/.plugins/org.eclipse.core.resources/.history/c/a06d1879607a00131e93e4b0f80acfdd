import javax.swing.JFrame;

import net.miginfocom.swing.MigLayout;


public class Test {
	
	//Constants
	private static int width = 15;
	private static int height = 30;
		
	private Segment segmentZero = new Segment(5, 0, height, width);
	private Segment segmentOne = new Segment(0, 5, width, height);
	private Segment segmentTwo = new Segment(0, 10, width, height);
	private Segment segmentThree = new Segment(5, 20, height, width);
	private Segment segmentFour = new Segment(0, 0, width, height);
	private Segment segmentFive = new Segment(0, 0, width, height);
	private Segment segmentSix = new Segment(0, 0, height, width);
	
	public static void main(String[] args) {
		JFrame f = new JFrame();
		
		f.setVisible(true);
		f.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
		f.setSize(600, 400);

		add(segmentZero);
		add(segmentOne);
		add(segmentTwo);
		add(segmentThree);
		add(segmentFour);
		add(segmentFive);
		add(segmentSix);
	}

}
